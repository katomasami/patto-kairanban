<h1>編集</h1>

<%= form_with model: @notice do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>本文</h4>
  <%= f.text_area :body %>
  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <%= f.submit "変更を保存" %>
<% end %>

  <%= link_to "戻る", notice_path(@notice) %>